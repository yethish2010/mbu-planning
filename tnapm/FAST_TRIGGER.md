# TNAPM accelerated Step-9 execution trigger

This marker triggers the 60-shard execution of the frozen 6,000-run TNAPM experiment. Only scheduling granularity changes: each shard retains the exact uploaded runner's backend, benchmark, method, seed, shot, candidate, compilation, noise-model, and metric logic.
